/* AUTOMATICALLY GENERATED by qapi-gen.py DO NOT MODIFY */

/*
 * Schema-defined QAPI visitors
 *
 * Copyright IBM, Corp. 2011
 * Copyright (C) 2014-2018 Red Hat, Inc.
 *
 * This work is licensed under the terms of the GNU LGPL, version 2.1 or later.
 * See the COPYING.LIB file in the top-level directory.
 */

#include "qemu/osdep.h"
#include "qapi/error.h"
#include "qapi-visit-uefi.h"
#include "qapi-features.h"

bool visit_type_UefiVariable_members(Visitor *v, UefiVariable *obj, Error **errp)
{
    bool has_time = !!obj->time;
    bool has_digest = !!obj->digest;

    if (!visit_type_str(v, "guid", &obj->guid, errp)) {
        return false;
    }
    if (!visit_type_str(v, "name", &obj->name, errp)) {
        return false;
    }
    if (!visit_type_int(v, "attr", &obj->attr, errp)) {
        return false;
    }
    if (!visit_type_str(v, "data", &obj->data, errp)) {
        return false;
    }
    if (visit_optional(v, "time", &has_time)) {
        if (!visit_type_str(v, "time", &obj->time, errp)) {
            return false;
        }
    }
    if (visit_optional(v, "digest", &has_digest)) {
        if (!visit_type_str(v, "digest", &obj->digest, errp)) {
            return false;
        }
    }
    return true;
}

bool visit_type_UefiVariable(Visitor *v, const char *name,
                 UefiVariable **obj, Error **errp)
{
    bool ok = false;

    if (!visit_start_struct(v, name, (void **)obj, sizeof(UefiVariable), errp)) {
        return false;
    }
    if (!*obj) {
        /* incomplete */
        assert(visit_is_dealloc(v));
        ok = true;
        goto out_obj;
    }
    if (!visit_type_UefiVariable_members(v, *obj, errp)) {
        goto out_obj;
    }
    ok = visit_check_struct(v, errp);
out_obj:
    visit_end_struct(v, (void **)obj);
    if (!ok && visit_is_input(v)) {
        qapi_free_UefiVariable(*obj);
        *obj = NULL;
    }
    return ok;
}

bool visit_type_UefiVariableList(Visitor *v, const char *name,
                 UefiVariableList **obj, Error **errp)
{
    bool ok = false;
    UefiVariableList *tail;
    size_t size = sizeof(**obj);

    if (!visit_start_list(v, name, (GenericList **)obj, size, errp)) {
        return false;
    }

    for (tail = *obj; tail;
         tail = (UefiVariableList *)visit_next_list(v, (GenericList *)tail, size)) {
        if (!visit_type_UefiVariable(v, NULL, &tail->value, errp)) {
            goto out_obj;
        }
    }

    ok = visit_check_list(v, errp);
out_obj:
    visit_end_list(v, (void **)obj);
    if (!ok && visit_is_input(v)) {
        qapi_free_UefiVariableList(*obj);
        *obj = NULL;
    }
    return ok;
}

bool visit_type_UefiVarStore_members(Visitor *v, UefiVarStore *obj, Error **errp)
{
    if (!visit_type_int(v, "version", &obj->version, errp)) {
        return false;
    }
    if (!visit_type_UefiVariableList(v, "variables", &obj->variables, errp)) {
        return false;
    }
    return true;
}

bool visit_type_UefiVarStore(Visitor *v, const char *name,
                 UefiVarStore **obj, Error **errp)
{
    bool ok = false;

    if (!visit_start_struct(v, name, (void **)obj, sizeof(UefiVarStore), errp)) {
        return false;
    }
    if (!*obj) {
        /* incomplete */
        assert(visit_is_dealloc(v));
        ok = true;
        goto out_obj;
    }
    if (!visit_type_UefiVarStore_members(v, *obj, errp)) {
        goto out_obj;
    }
    ok = visit_check_struct(v, errp);
out_obj:
    visit_end_struct(v, (void **)obj);
    if (!ok && visit_is_input(v)) {
        qapi_free_UefiVarStore(*obj);
        *obj = NULL;
    }
    return ok;
}

/* Dummy declaration to prevent empty .o file */
char qapi_dummy_qapi_visit_uefi_c;
